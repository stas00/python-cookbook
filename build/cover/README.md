This currently produces a **placeholder** cover (a simple gradient + title). It will be replaced by a designed cover later.

To rebuild:

```
python3 build_final_cover.py
```

Requirements: rsvg-convert (librsvg) + ImageMagick (magick); zopflipng optional (used to shrink PNGs further if present).
